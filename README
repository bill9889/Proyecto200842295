    Pequeño tutorial de git:
Crear una carpeta con mkdir e ingresar a ella:

$ git init $ git remote add origin git@github.com:bill9889/Proyecto200842295.git $ git pull origin master ===> traer todo de la nube $ git status /// Se hace cambios a los archivos $ git add Commands.txt ====> en lugar del name puede ser: (.) o ("*.txt") $ git commit -m "pequeño cambio" $ git push -u origin master =====> Subir los cambios a la nuve
